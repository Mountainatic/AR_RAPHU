# Protocol Revision v0.3.4

Revision:
v0.3.3 -> v0.3.4

Frozen findings:
- representation roles passed;
- implementation closure passed;
- mother-space full-kernel capacity passed 60/60;
- structural-space full-surface capacity passed 60/60;
- the original stop was caused only by applying a universal rank-2
  replacement gate to AR-S4U.

Correction:
- sigma2 significance is not equivalent to effective rank 2;
- AR-S4U is expected to have more than two important modes;
- exact algebraic rank is replaced by structural, predictive, and deployment
  effective ranks under preregistered error budgets.

Execution boundary:
- preserve every v0.3.3 result and decision as read-only evidence;
- reuse the frozen v0.3.3 full fits and selected smoothing values;
- run R0 -> E2A_SR -> E2A_SRB/E2A_P_NAT -> E2A_P_PERM in dependency order;
- never reselect basis or smoothing from truth or rank results;
- generate the v0.3.4 decision and package, then pause;
- do not implement or run E2B/E3 in this revision.
