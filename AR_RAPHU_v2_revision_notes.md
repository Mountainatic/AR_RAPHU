# AR-RAPHU v2 修订说明

本次修订基于以下新确认事实：

1. 当前 CZ 数据只来自一根硅直拉晶棒；
2. 数据只包含等径阶段；
3. 表格最后一列是晶体直径输出；
4. 原历史模型把过去一步或过去窗口内的晶体直径作为第十个输入通道；
5. 当前接口仍按 10 个通道处理；
6. 单位、采样周期、测径方式和传感器位置尚未取得；
7. 后续可能取得至少数根额外晶棒。

## 核心变化

### 原模型

\[
\widehat y_t
=
b+
\sum_{j=1}^{p}
\sum_\tau
q_j(\tau)f_j(x_{j,t-\tau}).
\]

该形式隐含所有输入都是普通过程变量。

### 新模型

\[
\widehat y_t
=
b+
\sum_{j=1}^{9}
\sum_\tau
q_j^x(\tau)f_j^x(x_{j,t-\tau})
+
\sum_{\ell=1}^{L_y}
q^y(\ell)f^y(y_{t-\ell}).
\]

因此模型正式升级为：

> 自回归秩自适应并联 Hammerstein–Urysohn 模型（AR-RAPHU）。

## 保留不变的理论主干

- 方案 A 是方案 B 的 rank-1 子模型；
- A 锚定的正交 B 残差；
- 固定时滞后的凸样条内层；
- Gram 白化 SVD；
- rank-1/rank-2/full-surface 复杂度选择；
- block bootstrap；
- 合成—公开—真实三层证据链。

## 被修改的内容

- 输入从“10 个普通变量”改为“9 个过程变量 + 历史输出”；
- 真实 CZ 由“潜在跨晶棒验证”改为“当前单晶棒等径阶段案例”；
- 新增 persistence、AR-only、X-only、XAR；
- 新增多预测步长；
- B 第一版只升级外生过程变量；
- 新增自回归多步稳定性；
- 氩气常量通道标记为当前数据不可辨识；
- 公共数据实验提前到真实 CZ 之前；
- 所有未运行实验明确标记为 NOT_YET_RUN；
- 多晶棒部分改为未来数据到达后的增量协议。

## 当前文件

- `AR_RAPHU_method_v2.md`
- `AR_RAPHU_three_layer_validation_plan_v2.md`

旧 v1 文件保留用于追踪设计演化，不再作为当前执行规范。
