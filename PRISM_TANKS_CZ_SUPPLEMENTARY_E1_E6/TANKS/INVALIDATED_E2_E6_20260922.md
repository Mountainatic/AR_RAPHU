# Superseded Tanks E2–E6 outputs

The E2–E6 folders below this historical `TANKS/` namespace were generated by
`PRISM_V211_CASCADED_TANKS_E2_E6_VALIDATED_EXTENSION_20260918_R2`. Its feature
builder used the latest input at `idx-1` and target `y[idx+1]`, an effective
two-sample/8-second lead, while labelling the task H16/64 seconds. E3, E4 and E6
also reused K-only metrics across conditions.

Those outputs are excluded from all current claims. Their corrected rerun is
under `../TANKS_H16_CORRECTED_R3/` and uses target `y[origin+16]`, common
dependency-safe support, independently fitted ablations and the authority
strict nested-OOF selector.
